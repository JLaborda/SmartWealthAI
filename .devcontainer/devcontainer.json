{
  "name": "SmartWealthAI",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/aws-cli:1": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "forwardPorts": [8501, 5000],
  "portsAttributes": {
    "8501": { "label": "Streamlit", "onAutoForward": "openBrowser" },
    "5000": { "label": "MLflow" }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.terminal.activateEnvironment": true
      }
    }
  },
  "postCreateCommand": "pipx install poetry && poetry config virtualenvs.in-project true && poetry install && bash .devcontainer/install-env-hook.sh",
  "remoteEnv": {
    "SIMFIN_API_KEY": "${localEnv:SIMFIN_API_KEY}"
  }
}
