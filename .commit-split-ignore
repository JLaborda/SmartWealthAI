# Paths never staged by /commit_split unless you override in chat.
# Gitignore-style patterns (one per line).

src/
notebooks/
pyproject.toml
poetry.lock

# Keep personal/raw datasets out of split commits while allowing versioned reference CSVs.
data/*
!data/reference/
!data/reference/**
